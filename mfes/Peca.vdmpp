class Peca

types

public String = seq of char;

public loc:: x: nat
						 y: nat

instance variables

Tipo: String;
Força: nat;
location: loc

operations

public Peca: String * nat * nat * nat ==> Peca
Peca(str,n, x1, y1) == (
	Tipo:= str;
	Força:= n;
	location:= mk_loc(x1,y1)
);

end Peca